import React from 'react';
import ThemeToggle from './ThemeToggle';

const DarkModeButton = () => {
    return (
        <ThemeToggle className="" label={false} />
    );
};

export default DarkModeButton; 